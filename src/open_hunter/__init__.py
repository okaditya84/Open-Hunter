"""Open Hunter — an honest, modular job-discovery pipeline.

Given a list of company names, it resolves each to its real website, finds
the careers page (preferring well-known Applicant Tracking System APIs for
accuracy), extracts every open role, filters them against your criteria with
an LLM, and writes a clean CSV you can apply from directly.
"""

__version__ = "1.0.0"
